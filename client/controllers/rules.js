var controller = angular.module('epicprog.controller.rules', []);
	

controller.controller('epicprog.controller.rules', ['$scope', '$state',
    function rulesCtl($scope, $state) {
		//$.mobile.loading('hide');
		//$state.go('rules');
		
//    	$scope.submitAdd = function() {
//    		GApi.executeAuth('balance','pot.getStatus', $scope.contact).then(function(resp) {
//            	$state.go('add');
//        	});
//		function() {
//			GApi.executeAuth('balance','pot.getStatus', $scope.contact).then(function(resp) {
//	        	$state.go('add');
//	    	});
//    	}
    }
]);